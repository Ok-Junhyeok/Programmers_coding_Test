def solution(keymap, targets):
    answer = []

    # 키맵의 배열을 반복(대전제)
    # 타겟s 배열을 반복
    # 타겟(타겟s의)의 문자열을 반복해서 키맵에서 찾기
    # 키맵 중 더 짧은거 쓰기

    for i in range(len(keymap)):
        for j in range(len(keymap[i])):
            i = 0


    return answer